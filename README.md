# obj-viewer (WIP)
An OBJ file viewer that uses software rasterization.

## Usage
Drag an OBJ file to the app window. The progrma will open the file.

### Remarks
- This program only supports files with only triangles as faces

